{
  "name": "historyict",
  "version": "0.1.0",
  "license": "MIT",
  "engines": {
    "node": "7.4.0"
  },
  "angular-cli": {},
  "styles": [],
  "scripts": {
    "ng": "ng",
    "start": "node server.js",
    "preinstall": "npm uninstall -g angular-cli && npm uninstall -g @angular/cli && npm cache clean && npm install -g @angular/cli@latest && rm -rf node_modules && npm install --save-dev @angular/cli@latest",
    "postinstall": "",
    "lint": "tslint \"src/**/*.ts\" --project src/tsconfig.json --type-check && tslint \"e2e/**/*.ts\" --project e2e/tsconfig.json --type-check",
    "test": "ng test",
    "pree2e": "webdriver-manager update --standalone false --gecko false",
    "e2e": "protractor"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {}
}
